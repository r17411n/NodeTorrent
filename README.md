first part is finished, just need to get rid of uncessary code and implement some methods so multiple magnetlinks can be stored at once for playback in a playlist or switchin
around with the magnetlinks that could be loaded next.

gather the movie name and duration and year old release from imdb or other website, and make an array with these aspects with the last entrance being the magnet link

maybe create a visual representation of the playlist that can also physically be dragged into order.

find a way to autmatically find subtitles if they are not already in the file that is being torrented.

make an app that contols all of this, a website, simple program, web service, the possibilities are endless..

integrate this with all other methods of finding movies on the internet.

combine them together.

create a program that indexes this all, sell this for a subscription across the world.
